// Kept in its own module so CreatePollContext can import it without pulling
// the whole LoginOptions component (and its deps) into the main bundle.
export const POST_LOGIN_REDIRECT_KEY = "plannio_post_login_redirect";
